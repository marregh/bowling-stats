# Runtime dependencies. Pinned to what the box currently runs -- the Ubuntu
# migration will rebuild this environment from here.
Flask==3.1.3
waitress==3.0.2
requests==2.33.1
beautifulsoup4==4.14.3
lxml==6.1.1
openpyxl==3.1.5

# capture.py only (the scoresheet-image path); the site itself does not need these.
numpy==2.4.4
Pillow==12.2.0
opencv-python==4.13.0
